# Phantom Magnetosphere (AI Voice Note App)

โปรเจคนี้เป็นแอปบันทึกเสียงอัจฉริยะ (AI Voice Note) ที่สามารถถอดเสียง (STT), สรุปความ, และจัดรูปแบบข้อความได้ โดยใช้ Google Gemini AI และเชื่อมต่อกับ Fittcore API

## 📋 โครงสร้างโปรเจค
- **Frontend**: React Native (Expo)
- **Backend**: Python (Flask)

---

## 🛠️ สิ่งที่ต้องติดตั้งก่อน (Prerequisites)
1.  **Node.js** (สำหรับรันแอปมือถือ)
2.  **Python 3.10+** (สำหรับรันเซิร์ฟเวอร์หลังบ้าน)
3.  **FFmpeg** (จำเป็นสำหรับแปลงไฟล์เสียงใน Backend)
    - Mac: `brew install ffmpeg`
    - Windows: โหลดจากเว็บและตั้งค่า Path
4.  **Expo Go** application บนมือถือ (iOS/Android)

---

## 🚀 ขั้นตอนการติดตั้ง (Setup Guide)

### 1. การติดตั้งฝั่ง Backend (Server)
ทำที่โฟลเดอร์ `backend/`

1.  เข้าไปที่โฟลเดอร์ backend
    ```bash
    cd backend
    ```
2.  สร้าง Virtual Environment (แนะนำ)
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # Mac/Linux
    # หรือ venv\Scripts\activate ใน Windows
    ```
3.  ติดตั้ง Libraries
    ```bash
    pip install -r requirements.txt
    ```
4.  **สร้างไฟล์ `.env`** ในโฟลเดอร์ `backend/` เพื่อใส่ Key ต่างๆ:
    ```env
    # backend/.env
    PORT=5001
    GEMINI_API_KEY=ใส่_GEMINI_API_KEY_ของคุณที่นี่
    
    # Fittcore Integration (ถ้ามี)
    FITTCORE_CLIENT_ID=...
    FITTCORE_CLIENT_SECRET=...
    FITTCORE_TEAM_ID=...
    ```
5.  รันเซิร์ฟเวอร์
    ```bash
    python app.py
    ```
    *จะเห็นข้อความว่า Running on http://0.0.0.0:5001*

---

### 2. การติดตั้งฝั่ง Frontend (Mobile App)
ทำที่โฟลเดอร์หลัก (Root)

1.  กลับมาที่หน้าบ้าน
    ```bash
    cd ..
    ```
2.  ติดตั้ง Packages
    ```bash
    npm install
    ```
3.  **สร้างไฟล์ `.env`** ที่หน้าบ้าน (Root) เพื่อระบุ IP ของเครื่อง Server:
    *ต้องใช้ IP ของเครื่องคอมฯ (เช่น 192.168.1.XX หรือ 172.20.10.XX)*
    ```env
    # .env
    EXPO_PUBLIC_API_URL=http://<IP_ADDRESS_OF_YOUR_PC>:5001
    ```
    *วิธีดู IP: Mac พิมพ์ `ipconfig getifaddr en0` ใน Terminal*

4.  รันแอป
    ```bash
    npx expo start --clear
    ```
5.  ใช้มือถือสแกน QR Code ผ่านแอป **Expo Go**
    *สำคัญ: มือถือและคอมพิวเตอร์ต้องเชื่อมต่อ WiFi วงเดียวกัน (หรือใช้ Hotspot)*

---

## ⚠️ ปัญหาที่พบบ่อย
- **App ฟ้องว่า Cannot Connect to Backend**: เช็คว่าใส่ IP ใน `EXPO_PUBLIC_API_URL` ถูกต้องหรือไม่ และเช็คว่าเปิด `python app.py` ทิ้งไว้หรือยัง
- **Backend Error "ffmpeg not found"**: ต้องติดตั้ง FFmpeg ในเครื่องคอมฯ ก่อน
- **Gemini Error 429/403**: Quota ของ API Key เต็ม หรือไม่ได้เปิดใช้งาน Billing (ถ้าใช้รุ่น Pro)

---
