# 📋 สรุปรายละเอียดหลังประชุม

หลังจากที่ผมได้ประชุมวันที่ผ่านมาครับ มีรายละเอียดดังนี้

---

## ✅ 1. หน้านับสินค้า - เปลี่ยนจาก SKU เป็น Barcode

**ความต้องการ:** หน้าจอมี SKU มีสินค้าชัดเจน (อยากเพิ่ม barcode นิดนึง) ที่เป็นหน้านับสินค้า ที่พนักงานได้รับมอบหมาย อยากได้รหัส barcode แสดงด้วยครับ เอา ลองเอา SKU ออกแล้วเปลี่ยนเป็นรหัส barcode

### ไฟล์ที่ต้องแก้ไข:

| ไฟล์                                                   | รายละเอียด                                                    |
| ------------------------------------------------------ | ------------------------------------------------------------- |
| `app/(mini-apps)/stock-counter/products/index.tsx`     | แสดงรายการสินค้าในแอป - เปลี่ยนจากแสดง SKU เป็น Barcode       |
| `app/(mini-apps)/stock-counter/products/details.tsx`   | หน้ารายละเอียดสินค้า - เปลี่ยนจาก SKU เป็น Barcode            |
| `app/(mini-apps)/stock-counter/products/completed.tsx` | หน้าสินค้าที่นับเสร็จ - เปลี่ยนจาก SKU เป็น Barcode           |
| `admin-web/app/dashboard/counting/page.tsx`            | หน้า counting sessions บน admin - เปลี่ยนจาก SKU เป็น Barcode |

### วิธีการแก้ไข:

```typescript
// ก่อน - แสดง SKU
<Text>SKU: {product.sku}</Text>

// หลัง - แสดง Barcode
<Text>Barcode: {product.barcode}</Text>
```

### สถานะ: ✅ เสร็จสิ้น

**ไฟล์ที่แก้ไขแล้ว:**

- `app/(mini-apps)/stock-counter/products/index.tsx` - เปลี่ยนจาก SKU เป็น Barcode
- `app/(mini-apps)/stock-counter/products/details.tsx` - เปลี่ยนจาก SKU เป็น Barcode
- `app/(mini-apps)/stock-counter/products/completed.tsx` - เปลี่ยนจาก SKU เป็น Barcode
- `admin-web/app/dashboard/counting/page.tsx` - เปลี่ยนจาก SKU เป็น Barcode

---

## ✅ 2. รายงานพฤติกรรมพนักงาน (Employee Behavior Report)

**ความต้องการ:** อยากได้หน้ารายงานพฤติกรรมพนักงานว่า พนักงานคนนี้ มีการอัปโหลดรูป location แปลกๆ ซึ่งอยู่กับที่ไม่ตรงกับเขา พนักงานคนนี้ทำของหายบ่อยสุด ประมานนี้ครับเหมือนจะมีแล้วไหม ไม่มั่นใจอาจจะต้องปรับเพิ่มครับ

### สถานะปัจจุบัน:

- ✅ มีหน้ารายงานของหายอยู่แล้วที่ `admin-web/app/dashboard/reports/page.tsx`
- ✅ มีการแสดง `topDiscrepancyUsers` (พนักงานที่ทำของหายบ่อย)
- ❌ ยังไม่มีการตรวจสอบ location ที่ผิดปกติ

### ไฟล์ที่ต้องสร้างใหม่:

| ไฟล์                                                         | รายละเอียด                               |
| ------------------------------------------------------------ | ---------------------------------------- |
| `admin-web/app/dashboard/reports/employee-behavior/page.tsx` | **ไฟล์ใหม่** - หน้ารายงานพฤติกรรมพนักงาน |

### สิ่งที่ต้องเพิ่ม:

#### 2.1 Location Anomaly Detection (ตรวจจับ Location ผิดปกติ):

- ตรวจสอบ location ที่ซ้ำกันหลายครั้ง (นับจากที่เดียวตลอด)
- ตรวจสอบ location ไม่ตรงกับสาขาที่สังกัด
- วิเคราะห์จาก field `location` ใน `CountingSession`

#### 2.2 ของหายบ่อย (High Discrepancy):

- รวบรวมจาก `discrepancy` ใน `countingSessions`
- จัดอันดับพนักงานที่มี discrepancy สูงสุด

### โครงสร้างข้อมูลที่ใช้:

```typescript
interface EmployeeBehaviorReport {
  userId: string;
  userName: string;
  branchName: string;
  // Location analysis
  uniqueLocations: number;
  suspiciousLocations: number; // locations ไม่ตรงสาขา
  // Discrepancy
  totalDiscrepancy: number;
  averageDiscrepancy: number;
  sessionCount: number;
  // Risk level
  riskLevel: "low" | "medium" | "high";
}
```

### สถานะ: ✅ เสร็จสิ้น

**ไฟล์ที่สร้างใหม่:**

- `admin-web/app/dashboard/reports/employee-behavior/page.tsx` - หน้ารายงานพฤติกรรมพนักงานพร้อมฟีเจอร์:
  - 📍 Location Anomaly Detection - ตรวจจับ location ที่ผิดปกติ
  - 📊 Discrepancy Tracking - ติดตามของหายบ่อย
  - 🔴 Risk Level Calculation - คำนวณระดับความเสี่ยง (Low/Medium/High)
  - 👤 Employee Detail Modal - ดูรายละเอียดพนักงานแต่ละคน
  - 🔍 Filter & Sort - กรองตามสาขา, ระดับความเสี่ยง, เรียงลำดับ

---

## ✅ 3. เชื่อมต่อ API ภายนอกสำหรับเปรียบเทียบสินค้า

**ความต้องการ:** หน้ารายงานการนับสินค้า จะมีการเชื่อมต่อกับ API ข้างนอกครับตอนนี้ เพราะว่าเราไม่มีสิทธิเข้าถึง เรามีแค่อ่านค่า แล้วเอา ค่าจากระบบเราไปเทียบว่า ค่าในระบบของเรา กับ จำนวนสินค้าในระบบเขา เป็นยังไง แล้วเอามาเปรียบเทียบครับว่า ค่าความต่างกันกี่ชิ้น

### ไฟล์ที่ต้องสร้าง/แก้ไข:

| ไฟล์                                                        | รายละเอียด                                        |
| ----------------------------------------------------------- | ------------------------------------------------- |
| `services/external-api.service.ts`                          | **ไฟล์ใหม่** - Service สำหรับเชื่อมต่อ API ภายนอก |
| `admin-web/lib/external-api.ts`                             | **ไฟล์ใหม่** - API Service สำหรับ admin-web       |
| `admin-web/app/dashboard/reports/stock-comparison/page.tsx` | **ไฟล์ใหม่** - หน้าเปรียบเทียบสต็อก               |
| `types/index.ts`                                            | เพิ่ม types สำหรับ External API response          |

### โครงสร้าง Service:

```typescript
// services/external-api.service.ts
interface ExternalStockData {
  productId: string;
  barcode: string;
  externalQty: number;  // จำนวนจากระบบ ERP ภายนอก
  lastUpdated: Date;
}

interface StockComparison {
  productId: string;
  productName: string;
  barcode: string;
  ourQty: number;       // จำนวนที่นับได้ในระบบเรา
  externalQty: number;  // จำนวนจากระบบ ERP
  difference: number;   // ค่าความต่าง
  status: 'match' | 'over' | 'short';
}

export const fetchExternalStock = async (barcode: string): Promise<ExternalStockData>;
export const compareStock = async (productId: string): Promise<StockComparison>;
```

### ⚠️ ข้อมูลที่ต้องการจากลูกค้า:

- [ ] URL ของ External API
- [ ] Authentication method (API Key, OAuth, etc.)
- [ ] รูปแบบ Request/Response ของ API
- [ ] สิทธิ์การเข้าถึง (read-only)

### สถานะ: ✅ เสร็จสิ้น (รอเชื่อมต่อ API จริง)

**ไฟล์ที่สร้างใหม่:**

- `services/external-api.service.ts` - Service สำหรับเชื่อมต่อ API ภายนอก (Mobile)
- `admin-web/lib/external-api.ts` - Service สำหรับ Admin Web
- `admin-web/app/dashboard/reports/stock-comparison/page.tsx` - หน้าเปรียบเทียบสต็อกพร้อมฟีเจอร์:
  - 📊 ตารางเปรียบเทียบ ระบบเรา vs ERP ภายนอก
  - 🔍 Filter ตาม status (Match/Over/Short)
  - 📈 Summary Statistics
  - 📥 Export CSV
  - 🔄 Mock Data สำหรับทดสอบ (เปิด/ปิดได้)

**⚠️ หมายเหตุ:** ใช้ Mock Data ก่อน รอข้อมูล API จริงจากลูกค้า

---

## ✅ 4. เพิ่มหัวข้อ "Series" ในข้อมูลสินค้า

**ความต้องการ:** เพิ่มหัวข้อ Series ในข้อมูลสินค้าในเว็บ และมือถือ ไม่ว่าจะเป็นเพิ่ม ลบ แก้ไข ครับ

### ไฟล์ที่ต้องแก้ไข:

| ไฟล์                                                 | รายละเอียด                                         |
| ---------------------------------------------------- | -------------------------------------------------- |
| `types/index.ts`                                     | เพิ่ม field `series` ใน `Product` interface        |
| `admin-web/app/dashboard/products/page.tsx`          | เพิ่ม input สำหรับ Series ในฟอร์มเพิ่ม/แก้ไขสินค้า |
| `app/(mini-apps)/stock-counter/products/index.tsx`   | แสดง Series ในรายการสินค้า                         |
| `app/(mini-apps)/stock-counter/products/details.tsx` | แสดง Series ในหน้ารายละเอียด                       |
| `scripts/seed-data.ts`                               | เพิ่ม Series ใน seed data                          |

### การแก้ไข Types:

```typescript
// types/index.ts
export interface Product {
  id: string;
  companyId: string;
  branchId?: string;
  sku: string;
  name: string;
  description?: string;
  barcode: string;
  category?: string;
  series?: string; // ← เพิ่มใหม่
  imageUrl?: string;
  createdAt: Timestamp;
  updatedAt?: Timestamp;
}
```

### การแก้ไขฟอร์ม (admin-web):

```typescript
// formData state
const [formData, setFormData] = useState({
  productId: "",
  name: "",
  description: "",
  barcode: "",
  category: "",
  series: "", // ← เพิ่มใหม่
  beforeCount: 0,
  companyId: "",
  branchId: "",
});
```

### สถานะ: ✅ เสร็จสิ้น

**ไฟล์ที่แก้ไขแล้ว:**

- `types/index.ts` - เพิ่ม field `series?: string` ใน Product interface
- `admin-web/app/dashboard/products/page.tsx` - เพิ่ม Series ในฟอร์มเพิ่ม/แก้ไขสินค้า:
  - ✅ เพิ่มใน formData state
  - ✅ เพิ่มใน handleAddProduct
  - ✅ เพิ่มใน handleEditProduct
  - ✅ เพิ่มใน handleUpdateProduct
  - ✅ เพิ่ม Input Field ใน Modal

---

## ✅ 5. การนับสินค้าเหมือนกันทุกคน + ฟังก์ชันข้ามสินค้า/เพิ่มสินค้า

**ความต้องการ:** ตอนนี้หน้า admin-web/app/dashboard/branches/[id]/page.tsx อาจจะไม่มี การแนบสินค้าให้รายบุคคลว่า แต่ละคนมีการนับสินค้าไม่เหมือนกัน แต่ เอาทุกคนทุกสาขา มีการนับสินค้าเหมือนกันหมดเลยแล้วกันครับ แล้วในแอปมือถืออาจจะบอกว่า สินค้านี้ข้ามไปได้ หรือเพิ่มสินค้าเพื่อถ่ายเอกได้ในกรณีไม่มีใน ฐานข้อมูลหน้าที่ ที่พนักงานต้องนับครับ

### สรุปความต้องการ:

1. **ทุกคนทุกสาขานับสินค้าชุดเดียวกัน** (ไม่ต้อง assign รายบุคคล)
2. **ข้ามสินค้าได้** ถ้าพนักงานไม่ต้องการนับสินค้านั้น
3. **เพิ่มสินค้าที่ไม่มีในระบบได้** ระหว่างการนับ

### ไฟล์ที่ต้องแก้ไข:

| ไฟล์                                                     | รายละเอียด                                              |
| -------------------------------------------------------- | ------------------------------------------------------- |
| `admin-web/app/dashboard/branches/[id]/page.tsx`         | ปรับโหมดการ assign จาก per-user เป็น branch-level       |
| `services/product.service.ts`                            | แก้ไขให้ดึงสินค้าตาม company/branch แทน user assignment |
| `app/(mini-apps)/stock-counter/products/index.tsx`       | เพิ่มปุ่ม "ข้ามสินค้า" + "เพิ่มสินค้าใหม่"              |
| `app/(mini-apps)/stock-counter/products/add-product.tsx` | **ไฟล์ใหม่** - หน้าเพิ่มสินค้าในแอปมือถือ               |
| `types/index.ts`                                         | เพิ่ม types สำหรับ skipped products                     |

### การเพิ่มปุ่มข้ามสินค้า (Mobile App):

```tsx
// ในหน้า products/index.tsx
<TouchableOpacity
  style={styles.skipButton}
  onPress={() => handleSkipProduct(product.productId)}
>
  <Ionicons name="arrow-forward" size={20} />
  <Text>ข้ามสินค้านี้</Text>
</TouchableOpacity>

<TouchableOpacity
  style={styles.addButton}
  onPress={() => router.push('/(mini-apps)/stock-counter/products/add-product')}
>
  <Ionicons name="add" size={20} />
  <Text>เพิ่มสินค้าใหม่</Text>
</TouchableOpacity>
```

### ฟังก์ชัน Skip Product:

```typescript
interface SkippedProduct {
  id: string;
  userId: string;
  productId: string;
  reason: string;
  skippedAt: Timestamp;
}

const handleSkipProduct = async (productId: string) => {
  // บันทึกว่าผู้ใช้ข้ามสินค้านี้
  await addDoc(collection(db, "skippedProducts"), {
    userId: user.uid,
    productId,
    reason: "user_skipped",
    skippedAt: serverTimestamp(),
  });
  // อัพเดท UI
};
```

### สถานะ: ✅ เสร็จสิ้น

**ไฟล์ที่แก้ไข/สร้างใหม่:**

- `types/index.ts` - เพิ่ม `SkippedProduct` interface
- `app/(mini-apps)/stock-counter/products/index.tsx`:
  - ✅ เพิ่ม `handleSkipProduct()` function
  - ✅ เพิ่มปุ่ม "ข้าม" ในรายการสินค้า
  - ✅ เพิ่มปุ่ม "+" ลอยสำหรับเพิ่มสินค้าใหม่
- `app/(mini-apps)/stock-counter/products/add-product.tsx` (**ไฟล์ใหม่**):
  - ✅ ฟอร์มเพิ่มสินค้าใหม่พร้อมถ่ายรูป
  - ✅ รองรับ field: ชื่อ, barcode, category, series, รูปภาพ
  - ✅ บันทึกลง Firebase พร้อม upload รูป

---

## 📊 สรุปภาพรวมการดำเนินการ

| ลำดับ | รายการ                          | ความซับซ้อน        | ประมาณเวลา | สถานะ                  |
| ----- | ------------------------------- | ------------------ | ---------- | ---------------------- |
| 1     | เปลี่ยน SKU เป็น Barcode        | ⭐ ง่าย            | 1-2 ชม.    | ✅ เสร็จสิ้น           |
| 2     | รายงานพฤติกรรมพนักงาน           | ⭐⭐⭐ ปานกลาง-ยาก | 4-6 ชม.    | ✅ เสร็จสิ้น           |
| 3     | เชื่อมต่อ API ภายนอก            | ⭐⭐⭐⭐ ยาก       | 8-16 ชม.   | ✅ เสร็จ (รอ API จริง) |
| 4     | เพิ่ม Series Field              | ⭐⭐ ง่าย-ปานกลาง  | 2-3 ชม.    | ✅ เสร็จสิ้น           |
| 5     | นับสินค้าเหมือนกัน + Skip + Add | ⭐⭐⭐ ปานกลาง     | 4-6 ชม.    | ✅ เสร็จสิ้น           |

---

## 🚀 ลำดับความสำคัญที่แนะนำ

1. **อันดับ 1:** เพิ่ม Series + เปลี่ยน SKU เป็น Barcode (ทำได้เลย ไม่ซับซ้อน)
2. **อันดับ 2:** ปรับระบบนับสินค้าให้ทุกคนเหมือนกัน + เพิ่มปุ่มข้าม/เพิ่มสินค้า
3. **อันดับ 3:** รายงานพฤติกรรมพนักงาน
4. **อันดับ 4:** เชื่อมต่อ API ภายนอก (ต้องรอ specification จากลูกค้าว่าจะเชื่อมกับระบบอะไร)

---

## 📝 หมายเหตุ

- วันที่ประชุม: ม.ค. 2026
- ผู้บันทึก: Developer Team
- สถานะ: ✅ ดำเนินการเสร็จสิ้น
- วันที่ดำเนินการ: ม.ค. 2026

---

## 🎉 สรุปการ Implementation

### ไฟล์ที่สร้างใหม่ทั้งหมด:

1. `admin-web/app/dashboard/reports/employee-behavior/page.tsx` - รายงานพฤติกรรมพนักงาน
2. `admin-web/app/dashboard/reports/stock-comparison/page.tsx` - หน้าเปรียบเทียบสต็อก
3. `admin-web/lib/external-api.ts` - Service สำหรับเชื่อมต่อ API ภายนอก (Admin)
4. `services/external-api.service.ts` - Service สำหรับเชื่อมต่อ API ภายนอก (Mobile)
5. `app/(mini-apps)/stock-counter/products/add-product.tsx` - หน้าเพิ่มสินค้าใหม่ในแอปมือถือ

### ไฟล์ที่แก้ไข:

1. `types/index.ts` - เพิ่ม `series` field และ `SkippedProduct` interface
2. `admin-web/app/dashboard/products/page.tsx` - เพิ่ม Series ในฟอร์ม CRUD
3. `app/(mini-apps)/stock-counter/products/index.tsx` - เปลี่ยน SKU→Barcode, เพิ่มปุ่ม Skip & Add
4. `app/(mini-apps)/stock-counter/products/details.tsx` - เปลี่ยน SKU→Barcode
5. `app/(mini-apps)/stock-counter/products/completed.tsx` - เปลี่ยน SKU→Barcode
6. `admin-web/app/dashboard/counting/page.tsx` - เปลี่ยน SKU→Barcode
