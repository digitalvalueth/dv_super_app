# ต่อไปผมจะทำเป็น แอปนึงหรือ ฟังก์ชันนึงดีครับ เพราะว่ามันเป็นส่วนนึงของการนับสินค้าด้วยอะครับ

แต่อันนี้เป็นการทำงานประมาณว่า

1. เมื่อมันจะมีโปรแกรมนึงของเขาแล้วเขาจะต้อง ตรวจสอบสถานะการส่งของว่าของถึงไหนแล้วพนะงานส่งรูปเข้าไป ในไลน์หรือยัง เหมือนเหมือนคล้ายโปรแกรมของที่ผมกำลังจะไปเชื่อมต่อของเขา ไม่รู้ว่ามันจะยุ่งยากไหมครับหมายความว่าผมอาจจะเอาบางสวิ่งบางอย่างที่ผมอาจจะไปเชื่อม API แต่ผมก็อาจจะทำเองก็ได้แต่อันนี้มันจะไปเชื่อมกับระบบคลังสินค้าเขาน่ะครับ

2. เอาเป้นวาตอนนี้ผมอยากได้ประมาณว่า เมื่อเขา tracking สินค้า สถานะ ว่าอยู่ไหน แล้วในแอปที่เราจะทำก็แค่ แต่ก่อนที่จะบอก พนักงานถ่ายรูปในไลน์ครับ ซึ่งผมมองว่า กว่าเขาระรวบรวมรูปภาพแลเวเขาก็ไปอัปเดทในระบบอีกทีผมมองว่ามันซ้ำซ้อน และอาจจะทำให้พนักงานเหนื่อย ผมก็เลยอยากให้เขาแนบรูปภาพบอกว่า สาขานี้มีสินค้ามาส่งนะ ของพนักงานคนนี้นะที่มาส่ง แล้วก็ให้ถ่ายยืนยันว่าสินค้าส่งมาแล้ว พร้อมกับแนบว่า ได้รับสินค้าเข้าสาขาแล้วอะไรประมาณนี้อ่ะครับ เราจะทำเว็ปเป็นตัวอย่างให้เขาดูก่อนก็ได้ครับ

3. เช็คชื่อพนักงาน ทุกๆวันพนักงานจะต้องเข้าทำงาน แล้วต้องถ่ายบอกว่า ตอนนี้เข้างานแล้ว เขาจะต้องจ่ายรูปที่เขายืนกับบูท ที่เขาขายอาหารเสิริมหรือผลิตพันของเขาน่ะครับ

---

# 📋 แผนพัฒนาระบบ - ข้อเสนอจาก Copilot

## 🎯 สรุปความต้องการ

จากที่อ่าน มี **3 ฟีเจอร์หลัก** ที่ต้องการ:

| #   | ฟีเจอร์               | รายละเอียด                                     |
| --- | --------------------- | ---------------------------------------------- |
| 1   | **Delivery Tracking** | ติดตามสถานะการส่งสินค้า (อาจเชื่อม API ภายนอก) |
| 2   | **รับสินค้าเข้าสาขา** | พนักงานถ่ายรูปยืนยันรับสินค้า แทนส่งรูปใน LINE |
| 3   | **เช็คชื่อพนักงาน**   | ถ่ายรูปยืนยันเข้างานทุกวัน พร้อมรูปบูธ         |

---

## 💡 คำแนะนำ: ควรทำเป็น Mini Apps แยก

### ❓ ทำไมไม่รวมกับ Stock Counter?

| เหตุผล                  | อธิบาย                                                 |
| ----------------------- | ------------------------------------------------------ |
| **Workflow ต่างกัน**    | Stock Counter = นับสินค้า, ฟีเจอร์ใหม่ = ถ่ายรูปยืนยัน |
| **User ต่างกัน**        | Check-in อาจใช้ทุกคน, Stock Counter เฉพาะคนนับ         |
| **ง่ายต่อการ Maintain** | แยกกันดูแลและอัพเดทได้อิสระ                            |
| **เปิด/ปิดได้**         | บางบริษัทอาจไม่ต้องการทุกฟีเจอร์                       |

---

## 🏗️ โครงสร้างที่เสนอ

### Option A: แยกเป็น 2 Mini Apps (แนะนำ ✅)

```
app/(mini-apps)/
├── stock-counter/          # ✅ มีอยู่แล้ว - นับสต็อก
├── delivery-receive/       # 🆕 รับสินค้าเข้าสาขา + Tracking
│   ├── index.tsx          # หน้าหลัก
│   ├── receive.tsx        # ถ่ายรูปรับสินค้า
│   ├── history.tsx        # ประวัติการรับ
│   └── tracking.tsx       # ติดตามสถานะ (Phase 2)
└── check-in/              # 🆕 เช็คชื่อพนักงาน
    ├── index.tsx          # หน้าหลัก
    ├── camera.tsx         # ถ่ายรูป check-in
    └── history.tsx        # ประวัติ check-in
```

### Option B: รวมทั้งหมดใน Stock Counter

```
app/(mini-apps)/stock-counter/
├── products/              # นับสต็อก (มีอยู่)
├── receive/               # 🆕 รับสินค้า
├── check-in/              # 🆕 เช็คชื่อ
└── history/               # ประวัติรวม
```

**👉 แนะนำ Option A** เพราะแยกชัดเจน ง่ายต่อการพัฒนาและ maintain

---

## 📱 Mini App 1: รับสินค้าเข้าสาขา (Delivery Receive)

### Workflow

```
พนักงานส่ง → ถึงสาขา → พนักงานสาขาถ่ายรูป → ยืนยันรับ → บันทึก+แจ้ง Admin
```

### หน้าจอหลัก

| หน้า          | ฟังก์ชัน                                         |
| ------------- | ------------------------------------------------ |
| **หน้าหลัก**  | แสดงรายการที่รอรับ / รับแล้ววันนี้               |
| **รับสินค้า** | เลือก shipment → ถ่ายรูป → กรอกหมายเหตุ → บันทึก |
| **ประวัติ**   | ดูรายการที่รับแล้ว + รูปภาพ                      |
| **Tracking**  | (Phase 2) ดูสถานะจาก API ภายนอก                  |

### ข้อมูลที่บันทึก

```typescript
interface DeliveryReceive {
  id: string;
  companyId: string;
  branchId: string;
  branchName: string;

  // ข้อมูลการส่ง
  shipmentId?: string; // รหัสการส่ง (ถ้ามี)
  deliveryPersonName: string; // ชื่อพนักงานส่ง
  deliveryCompany?: string; // บริษัทขนส่ง

  // ข้อมูลการรับ
  receivedBy: string; // UID พนักงานรับ
  receivedByName: string;
  receivedAt: Timestamp;

  // รูปภาพ + Watermark
  imageUrl: string;
  watermarkData: {
    timestamp: string;
    location: string;
    coordinates: { lat; lng };
    employeeName: string;
    deviceModel: string;
  };

  remarks?: string;
  status: "received" | "verified" | "issue";
}
```

---

## 📱 Mini App 2: เช็คชื่อพนักงาน (Check-in)

### Workflow

```
พนักงานเปิดแอป → ถ่ายรูปยืนกับบูธ → ระบบบันทึก + ฝัง Watermark → Admin ดูรายงาน
```

### หน้าจอหลัก

| หน้า         | ฟังก์ชัน                                |
| ------------ | --------------------------------------- |
| **หน้าหลัก** | ปุ่ม Check-in / Check-out + สถานะวันนี้ |
| **ถ่ายรูป**  | ถ่ายรูปยืนกับบูธ + ฝัง Watermark        |
| **ประวัติ**  | ดูประวัติ check-in ของตัวเอง            |

### ข้อมูลที่บันทึก

```typescript
interface CheckIn {
  id: string;
  userId: string;
  userName: string;
  companyId: string;
  branchId: string;
  branchName: string;

  type: "check-in" | "check-out";

  // รูปภาพ + Watermark
  imageUrl: string;
  watermarkData: {
    timestamp: string;
    location: string;
    coordinates: { lat; lng };
    employeeName: string;
    deviceModel: string;
  };

  // ข้อมูลเพิ่มเติม
  isLate?: boolean; // มาสาย?
  lateMinutes?: number;
  remarks?: string;

  createdAt: Timestamp;
}
```

---

## 🖥️ Admin Dashboard (เพิ่มเติม)

### หน้าที่ต้องเพิ่ม

```
admin-web/app/dashboard/
├── delivery/              # 🆕 จัดการรับสินค้า
│   ├── page.tsx          # รายการทั้งหมด
│   └── [id]/page.tsx     # รายละเอียด
└── attendance/           # 🆕 รายงานเช็คชื่อ
    ├── page.tsx          # สรุปรายวัน
    ├── report.tsx        # รายงานรายเดือน
    └── [userId]/page.tsx # ประวัติรายคน
```

---

## 📅 แผนการพัฒนา (เสนอ)

### Phase 1: Check-in (1-2 สัปดาห์)

- [ ] สร้าง Mini App check-in
- [ ] หน้าถ่ายรูป + Watermark (ใช้ code จาก stock-counter)
- [ ] หน้าประวัติ
- [ ] Admin Dashboard: หน้ารายงานเช็คชื่อ

### Phase 2: รับสินค้าเข้าสาขา (1-2 สัปดาห์)

- [ ] สร้าง Mini App delivery-receive
- [ ] หน้ารับสินค้า + ถ่ายรูป
- [ ] หน้าประวัติ
- [ ] Admin Dashboard: หน้าจัดการรับสินค้า

### Phase 3: Delivery Tracking (Optional)

- [ ] เชื่อม API ภายนอก (ถ้ามี)
- [ ] หน้า Tracking สถานะ

---

## ❓ คำถาม & คำตอบ

| คำถาม                               | คำตอบ                                            |
| ----------------------------------- | ------------------------------------------------ |
| **Check-in มีกำหนดเวลาเข้างานไหม?** | ✅ สายหลัง **10:00 น.**                          |
| **Check-out ต้องมีด้วยไหม?**        | ✅ ทำไว้ก่อนได้ (optional)                       |
| **รับสินค้า - ต้องระบุรายการไหม?**  | ✅ Mock data ก่อน - สุ่ม tracking + สินค้าจาก DB |
| **มี API คลังสินค้าพร้อมหรือยัง?**  | ⏳ จะไปคุยพรุ่งนี้ (23 ม.ค.)                     |

---

## 🚀 แผนการทำงาน (อัพเดท)

### ✅ เริ่มทำได้เลย: Check-in พนักงาน

เพราะไม่ต้องรอ API ภายนอก และใช้ code จาก stock-counter ได้เยอะ

#### สิ่งที่จะทำ:

```
📱 Mobile App
├── /(mini-apps)/check-in/
│   ├── index.tsx         # หน้าหลัก (ปุ่ม Check-in / Check-out)
│   ├── camera.tsx        # ถ่ายรูป + Watermark
│   └── history.tsx       # ประวัติ check-in ของตัวเอง

🖥️ Admin Dashboard
├── /dashboard/attendance/
│   ├── page.tsx          # สรุปรายวัน (ใครมา/ไม่มา/สาย)
│   └── [userId]/page.tsx # ประวัติรายคน
```

#### Logic เวลาสาย:

```typescript
const WORK_START_TIME = "10:00"; // เวลาเข้างาน

function checkIfLate(checkInTime: Date): {
  isLate: boolean;
  lateMinutes: number;
} {
  const [hour, minute] = WORK_START_TIME.split(":").map(Number);
  const workStart = new Date(checkInTime);
  workStart.setHours(hour, minute, 0, 0);

  if (checkInTime > workStart) {
    const lateMinutes = Math.floor(
      (checkInTime.getTime() - workStart.getTime()) / 60000,
    );
    return { isLate: true, lateMinutes };
  }
  return { isLate: false, lateMinutes: 0 };
}
```

---

### ⏳ ทำหลังจากคุย API: รับสินค้าเข้าสาขา

#### Mock Data Strategy (ระหว่างรอ API จริง):

```typescript
// สร้าง Mock Shipment จากสินค้าที่มีใน DB
interface MockShipment {
  id: string;
  trackingNumber: string; // สุ่ม: "TH" + random 12 digits
  products: Product[]; // ดึงจาก products collection
  assignedBranchId: string;
  assignedEmployeeId: string;
  status: "pending" | "in_transit" | "delivered";
  estimatedDelivery: Date;
}

// ตัวอย่างการสร้าง mock
function generateMockShipment(branchId: string): MockShipment {
  const trackingNumber = `TH${Date.now()}${Math.random().toString().slice(2, 8)}`;
  // ดึงสินค้าจาก DB มา 3-5 รายการ
  // assign ให้พนักงานในสาขานั้น
}
```

---

## 📋 Checklist สำหรับ Check-in (Phase 1)

### Mobile App

- [ ] สร้าง `/(mini-apps)/check-in/_layout.tsx`
- [ ] สร้าง `/(mini-apps)/check-in/index.tsx` - หน้าหลัก
- [ ] สร้าง `/(mini-apps)/check-in/camera.tsx` - ถ่ายรูป
- [ ] สร้าง `/(mini-apps)/check-in/history.tsx` - ประวัติ
- [ ] สร้าง `services/checkin.service.ts`
- [ ] สร้าง `stores/checkin.store.ts`
- [ ] เพิ่มปุ่มเข้า Check-in ในหน้า Services

### Admin Dashboard

- [ ] สร้าง `/dashboard/attendance/page.tsx` - สรุปรายวัน
- [ ] สร้าง `/dashboard/attendance/[userId]/page.tsx` - ประวัติรายคน
- [ ] เพิ่มเมนู "เช็คชื่อพนักงาน" ใน Sidebar

### Database (Firestore)

- [ ] Collection: `check_ins`
- [ ] Collection: `attendance_settings` (เวลาเข้างาน ฯลฯ)

---

## 🎯 พร้อมเริ่มเลยไหมครับ?

บอกผมได้เลยว่า **"เริ่มเลย"** แล้วผมจะเริ่มสร้าง Check-in Mini App ให้ครับ! 🚀
